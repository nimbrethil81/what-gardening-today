# Project Specification: "What Gardening Today?"

## 1. VISION & STRATEGY

The "What Gardening Today?" app eliminates cognitive overload and decision paralysis for enthusiastic novice gardeners. Instead of navigating complex canvas designers, tracking layouts, or reading encyclopedias, the entire application behaviour is driven by a single core interface interaction: tapping a button to answer, "What gardening should I do today?"

### Core Principles
* **Action-Oriented:** Delivers immediate, hyper-localized, and time-appropriate tasks.
* **Novice-Friendly:** Strips away technical botanical jargon in favour of bite-sized, actionable guidance.
* **Zero-Maintenance Infrastructure:** Built with a fully decoupled, open web architecture that costs $0 to run and avoids platform lock-in.

---

## 2. COMPONENT ARCHITECTURE

The system is a Progressive Web App (PWA) reading dynamically from a relational database schema structured inside Google Sheets.

* **Data Layer (Google Sheets):** The relational database. Four primary tables: `User_Profile`, `Item_Dictionary`, `Master_Task_Matrix`, and `Task_Log`. A supporting `Reference_Lists` sheet maps display categories to their `Asset_ID` prefixes.
* **API Gateway (Google Apps Script Web App):** Listens for client GET/POST requests, filters data (season, cooldowns, weather), securely proxies the external weather API, and returns RESTful JSON payloads.
* **Frontend Interface (GitHub Pages Static Host):** Vanilla HTML5, CSS3, and JavaScript (`index.html`, `style.css`, `app.js`) configured with a Service Worker (`sw.js`) and Web App Manifest (`manifest.json`) to run as a standalone iOS/Android PWA. PWA icon and favicon assets are stored under `assets/icons/`.

---

## 3. RELATIONAL DATABASE SCHEMA (GOOGLE SHEETS)

### Asset ID Convention

All physical inventory items use the format `PREFIX[_SUBTYPE]_NNNN`:

* **PREFIX** — Top-level category prefix. Currently in use: `LAWN`, `BED`, `TREE`, `SHRUB`, `PLANT`, `VEG`, `HERB`, `STRUCT`, `TOOL`. `Reference_Lists` is the authoritative mapping between display categories and prefixes.
* **SUBTYPE (optional)** — One or more underscore-separated tokens describing the item type (e.g. `TOMATO`, `RAISED`, `MIXED_UTILITY`, `LILY_OF_THE_VALLEY`, `FRUIT_RASPBERRY`). There is no limit on the number of tokens.
* **NNNN** — Four-digit random numeric suffix, generated by the backend when a user adds an item. Ensures each specific instance has a globally unique ID.

Examples:
* `LAWN_MIXED_UTILITY_8258` — a specific mixed-utility lawn
* `VEG_TOMATO_3469` — a specific tomato plant
* `PLANT_LILY_OF_THE_VALLEY_1179` — a specific lily of the valley

### Three-Tier Task Matching

For every active item in `User_Profile`, the backend collects **three kinds of label**:

1. **Category tier** — the first segment of the `Asset_ID`. `PLANT_LILY_OF_THE_VALLEY_1179` → `PLANT`.
2. **Specific-item tier** — the entire `Asset_ID` minus its four-digit instance suffix. `PLANT_LILY_OF_THE_VALLEY_1179` → `PLANT_LILY_OF_THE_VALLEY`. This is the item's `Default_Asset_ID_Prefix` from `Item_Dictionary`, however many tokens it contains.
3. **Group tier** — any `GROUP_*` tags declared against that item's blueprint in the `Item_Dictionary.Groups` column. `VEG_FRUIT_RASPBERRY` → `GROUP_SOFT_FRUIT`.

A task is shown when its `Target_Asset_ID` is **exactly equal** to any label in that set. Matching is an equality test, never a "starts with" test.

This has three important consequences:

* **Prefixes may safely be substrings of one another.** `PLANT_LILY` and `PLANT_LILY_OF_THE_VALLEY` are entirely independent targets; neither can capture the other's tasks. (Historically the matcher truncated every asset to its first two segments, which caused `PLANT_LILY_OF_THE_VALLEY` items to receive true-lily tasks and to silently miss their own. See CHANGELOG 1.3.)
* **There is no implicit middle tier.** A target such as `VEG_FRUIT` or `VEG_BRASSICA` matches nothing, because no item ever registers a partial prefix. Groupings that sit between "whole category" and "one specific item" must be declared explicitly using the group tier — they can never be inferred from how a prefix happens to be spelled.
* **Groups are declared, not derived.** Group membership is a property of the dictionary *blueprint*, not of a user's individual garden item, and not of the item's name.

`_NNNN` instance suffixes are never used in `Target_Asset_ID`. `GROUP_` is a reserved prefix and must never begin an `Asset_ID` prefix.

### Tab 1: `User_Profile`

The physical features, assets, or plants currently present in the user's specific garden.

* **Asset_ID (Primary Key):** Unique uppercase identifier following the Asset ID Convention above.
* **Category:** Broad grouping used for UI display. Exactly one of the seven values in `Reference_Lists`: `Lawn`, `Beds`, `Trees & shrubs`, `Plants & flowers`, `Veg & herbs`, `Garden structures`, `Tools`. Where a blueprint appears under more than one category, this records the category tile the user actually added the item from.
* **Friendly_Name:** Optional human-readable name entered by the user (e.g. "Front lawn").
* **Is_Active:** `TRUE` / `FALSE` (soft-delete flag — inactive assets remain in the sheet but generate no tasks).

### Tab 2: `Item_Dictionary`

The global catalogue of item blueprints used to populate the "Add to My Garden" UI. **One blueprint per real-world item** — the same plant must never appear twice under different prefixes.

* **Category:** One **or more** of the seven allowed category values, comma-separated within the single cell (e.g. `Plants & flowers, Trees & shrubs`). The backend emits one picker entry per listed category, all pointing at the same blueprint, so an item such as Rose can appear under both tiles without being duplicated in the data.
* **Suggested_Name:** Human-readable label shown to the user in the pill selector (e.g. "Tomato", "Raised bed", "Lily of the Valley").
* **Default_Asset_ID_Prefix:** The `PREFIX[_SUBTYPE]` portion assigned to any instance the user creates from this blueprint. The backend appends `_NNNN` when the instance is created. Determines which category-tier and specific-tier tasks the item inherits.
* **Groups:** Optional. Zero or more `GROUP_*` tags, comma-separated (e.g. `GROUP_SOFT_FRUIT`). Declares the item's membership of cross-cutting groups that tasks may target. Blank means the item belongs to no groups.

### Tab 3: `Master_Task_Matrix`

The global engine containing the rules and care instructions for all supported garden assets.

* **Task_ID (Primary Key):** Unique opaque identifier (e.g. `TASK_0001`). Sequential four-digit numbering, immutable once assigned and never reused.
* **Target_Asset_ID:** Exactly one of: a category prefix (`LAWN`), a full item prefix present in `Item_Dictionary` (`VEG_FRUIT_RASPBERRY`), or a `GROUP_*` tag declared in `Item_Dictionary.Groups` (`GROUP_SOFT_FRUIT`).
* **Task_Name:** Brief title (e.g. "Spring Mowing").
* **Category:** Matches the target asset's category.
* **Instruction:** Detailed, step-by-step novice-friendly guidance.
* **Valid_Months:** Comma-separated month numbers when the task can occur (e.g. `3,4,5`).
* **Frequency_Days:** Cooldown in days before a completed task can reappear (e.g. `7` = weekly, `365` = annual).
* **Suppress_If_Raining:** `TRUE` = hide this task when it is currently raining at the user's location. `FALSE` or blank = ignore.
* **Suppress_If_Temp_Below:** Numeric temperature threshold in °C. If the current temperature is below this value, the task is hidden. Blank = ignore.
* **Requires_Wind_Above:** Numeric wind speed threshold. Behaviour of this column is a **known limitation** — see §5E.
* **Estimated_Minutes:** Whole number of minutes a novice would typically spend on one occurrence of the task (e.g. `30`). Returned in the API payload but not yet displayed in the UI.

**Category-tier safety rule.** A task targeting a bare category prefix must be correct for *every* member of that category, without exception. Where advice is safe for most but harmful to some members (e.g. cutting a woody plant to the ground, or feeding a plant that requires poor soil), it must be targeted at a group or a specific item instead.

### Tab 4: `Task_Log`

Append-only historical record of completed tasks.

* **Log_ID:** Auto-generated unique row ID (timestamp-based).
* **Task_ID:** References the completed `Master_Task_Matrix` row.
* **Date_Completed:** Date the task was ticked off. Stored as a UTC date — see known limitation in §5E.
* **Notes:** Default tracking string sent by the client (currently `"Completed via PWA client"`).

---

## 4. API ROUTES

The Apps Script Web App is the sole external API. All requests use the deployed Web App URL as the base.

### GET routes (identified by `?action=` query parameter)

* **`get_all`** — Combined payload: user inventory, filtered task list (season + cooldown + weather), item blueprint catalogue. Accepts `lat` and `lon` query parameters. **Primary endpoint used by the frontend.**
* **`get_tasks`** — Filtered task list only (season + cooldown, **no weather filtering**). Legacy route retained for compatibility — see known limitation in §5E.
* **`get_profile`** — User inventory only. Legacy route retained for compatibility.
* **`get_dictionary`** — Item blueprint catalogue only. Legacy route retained for compatibility.
* **`get_weather`** — Current weather data proxied from OpenWeather. Accepts `lat` and `lon`.

All GET requests append a client-supplied `&t=<timestamp>` cache-buster to force fresh evaluations on tab switches.

Asset matching, group resolution, cooldown suppression, and weather suppression are implemented **once**, in shared helper functions, and used by both `get_tasks` and `get_all`. The two routes previously carried independent copies of this logic, which allowed the same defect to exist in two places simultaneously; they must not be allowed to diverge again.

### POST routes (identified by `action` field in JSON body)

* **`add_asset`** — Creates a new `User_Profile` row from a chosen `Item_Dictionary` blueprint. Payload includes the target category, the chosen suggested name, and an optional custom reference name. The backend generates the `_NNNN` suffix and assigns `Is_Active = TRUE`.
* **`remove_asset`** — Soft-deletes an inventory row by setting `Is_Active = FALSE`. Payload includes the target `Asset_ID`.
* **Task logging (default POST)** — Any POST without a recognised `action` field is treated as a task completion event and appended to `Task_Log`. Payload includes the completed `Task_ID`.

---

## 5. ARCHITECTURAL PRINCIPLES & KNOWN LIMITATIONS

### A. Data Architecture & Future-Proofing

* **Blueprints, Not Instances:** The master database contains only generic, universally applicable data. User-specific data lives entirely in `User_Profile`.
* **Scalable Structures First:** Before adding new properties or columns, evaluate the structure for scale. Avoid hardcoding specific IDs where a broader category allows future flexibility.
* **Single Source of Truth:** Data must not be duplicated across sheets, and not within a sheet either. One blueprint per real-world item; where an item legitimately belongs in two places, express that with a multi-valued field rather than a second row.
* **Identifiers Are Not Taxonomy:** An `Asset_ID` prefix identifies an item; it does not encode a hierarchy that software may infer. Relationships between items (groupings, families, shared care needs) must be declared in their own column and matched explicitly. Deriving meaning from the internal structure of an identifier is the "smart key" antipattern and was the root cause of the matching defects fixed in 1.3.

### B. Separation of Concerns

* **Database (Google Sheets):** Strictly passive storage. No calculations or formatting.
* **API/Middleware (Apps Script):** Handles data retrieval, logical filtering (asset matching, dates, weather), and the secure weather proxy.
* **Frontend (GitHub Pages PWA):** Handles state management, UI logic, and view rendering. UI design elements (colours, formatting) never come from the database layers.
* **Three columns, three jobs.** `Default_Asset_ID_Prefix` drives task matching; `Category` drives UI display; `Groups` drives group-level task matching. None of these may be inferred from either of the others.

### C. Development & Error Handling

* **Fail Gracefully:** If an external resource fails, the app disables affected UI and displays a clear user-friendly error rather than crashing.
* **Iterative Commits:** Code changes to GitHub represent single, testable features or fixes to make rollback straightforward.
* **Property Name Normalisation:** Apps Script returns lowercase property names; frontend code normalises casing at the fetch layer. Casing mismatches between backend and frontend have caused real bugs — normalise once at ingestion rather than at each site of use.
* **No Duplicated Logic Across Routes:** Where two routes need the same rule, it lives in one shared function. Copy-pasted logic has already produced identical bugs in two places.
* **Service Worker Cache Versioning:** The service worker uses a network-first fetch strategy and a versioned cache name (`CACHE_NAME`). Every deploy that changes a cached frontend file must bump the version string (e.g. `gardening-v2` → `gardening-v3`); this triggers cleanup of stale caches on activation so deployed fixes reach installed PWAs. The app falls back to the cached copy only when the network is unavailable (offline use).

### D. AI Collaboration Workflow

1. **Primary Workspace Continuity.** The current conversation is the primary workspace across design discussions, bug fixes, and feature additions. Do not recommend starting a new conversation as routine best practice. A new conversation is only appropriate when: (a) the conversation has become so long that context is clearly being lost or responses are becoming incomplete; (b) a separate, largely independent workstream is being started (e.g. a major research task, design exploration, or documentation project); or (c) the developer explicitly requests one. If a new conversation is recommended, briefly explain why first.

2. **Design Before Implementation.** Before implementing a feature or bug fix, finalise the proposed design, then recommend the most appropriate Claude model and reasoning effort for implementation. Unless one of the workflow exceptions above applies, implementation continues in the current conversation regardless of that recommendation.

3. **Complete Files, Not Snippets.** When implementing a feature or bug fix, provide the complete contents of every file that changes. Do not provide partial snippets or diffs unless specifically requested.

4. **Documentation Review.** After implementing a feature or bug fix, review whether SPEC.md and CHANGELOG.md should be updated. If updates are appropriate, explain what should change and ask for confirmation before modifying either file.

5. **Plain English Communication.** When asking the developer to make a design decision, explain the options in terms of what they would actually see or experience, not technical shorthand. When providing test steps, write them as actions a non-developer could follow — what to do and what to expect to happen, not descriptions of underlying technical behaviour.

6. **Content Authoring.** Adding items to `Item_Dictionary` and tasks to `Master_Task_Matrix` follows the semicolon-import process, prompts, and verification steps documented in `docs/DATABASE_WORKFLOW.md`.

### E. Known Limitations

Documented so they aren't lost between sessions. None are urgent; each will be addressed when it makes sense to bundle with related work.

* **Weather location is hardcoded on the backend.** The `get_all` route filters tasks against the weather at Amersham (`GARDEN_LAT` / `GARDEN_LON`) regardless of the `lat`/`lon` the client sends. This is architecturally correct for a fixed-location, single-garden app, but the frontend widget reflects the user's actual device location, so widget and task filter can disagree when the user is away from home.
* **Wind suppression logic is likely inverted.** `Requires_Wind_Above` currently shows a task only when wind exceeds the threshold. The intended gardening use case is the opposite (hide spraying and feeding tasks when it is windy). To be corrected.
* **Two parallel data-fetch paths have drifted.** Initial load uses `get_all` (with weather filtering). Tabbing back to Today's Tasks re-fetches via `get_tasks` (without weather filtering), so weather-suppressed tasks temporarily reappear until the next full reload.
* **UTC midnight edge case.** `Date_Completed` is stamped in UTC. During British Summer Time, a task completed between midnight and 1am local is logged with yesterday's date.
* **`Task_Log` grows unboundedly.** No archiving mechanism. Not a problem at current scale but should be addressed before response times noticeably degrade.
* **Legacy two-digit asset ID suffixes.** Earlier assets used `_NN` suffixes (e.g. `LAWN_01`). The current display-name resolution assumes four-digit suffixes. Any residual two-digit-suffix items will fall back to raw ID or friendly name rather than a dictionary-derived display name.
* **Duplicate `Task_ID` `TASK_0262`.** Assigned to both a Thalictrum task and a Tiarella task, violating the primary-key rule. No runtime impact today, but one must be renumbered.
* **Woody plants catalogued under the `PLANT` prefix.** Clematis, Bamboo, Ivy, Jasmine, Rhododendron, Viburnum, Heather and Fuchsia are woody or sub-shrubby but carry `PLANT_*` prefixes, so they inherit generic `PLANT` care. The genuinely damaging generic tasks have been retargeted away from the category tier (see 1.3), so this is currently cosmetic — but these blueprints belong under `SHRUB_*`.
* **Retired Task IDs.** `TASK_0050` and `TASK_0051` have been deleted (see CHANGELOG 1.3). Per the immutable primary-key rule these numbers are retired and must never be reissued.
* **Items with no applicable tasks.** `STRUCT_WATER_BUTT` and `STRUCT_COMPOST_BIN` receive no tasks: no specific tasks have been authored and there is no generic `STRUCT` tier.

---

## 6. DEVELOPMENT ROADMAP

* **Phase 1 (COMPLETE):** Operational single-button PWA. Logic matches current-month strings on the Google Sheets backend and returns basic daily tasks.
* **Phase 2 (COMPLETE):** Stateful interactivity. Checkboxes on task cards; completion writes to `Task_Log` via POST. Processing engine calculates `Date_Completed + Frequency_Days` to suppress completed tasks for their cooldown window.
* **Phase 2.1 (COMPLETE):** Dynamic inventory management. "My Garden" tab with category tiles, item pill selector, custom-name input, and soft-delete confirmation flow. `Item_Dictionary` drives pill population; asset matching applies user-added items to relevant tasks.
* **Phase 3 (COMPLETE):** Environmental API integration. HTML5 Geolocation on the frontend widget; secure OpenWeather API proxy through Apps Script; season and weather task filtering combined in the `get_all` route.
* **Phase 4 (PENDING):** Cross-platform scale. Migrate the database from Google Sheets to a hosted PostgreSQL instance (e.g. Supabase), add user authentication for multi-tenant support, and evaluate whether a native rewrite (Flutter / SwiftUI) is warranted beyond the current PWA.
